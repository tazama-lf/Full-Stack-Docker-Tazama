# base infrastructure

services:
  valkey:
    hostname: valkey
    image: valkey/valkey:7.2.5
    restart: always
    command: "valkey-server --port 6379 --loglevel verbose"
    healthcheck:
      test: ["CMD-SHELL", "valkey-cli -p 6379 ping | grep PONG"]
      interval: 1s
      timeout: 3s
      retries: 5

  postgres:
    image: postgres:18
    restart: always
    volumes:
      - ./postgres/migration/base/rls.sql:/tmp/rls.sql
      - ./postgres/migration/base/00-DB.sql:/docker-entrypoint-initdb.d/00-DB.sql
      - ./postgres/migration/base/01-RLS.sh:/docker-entrypoint-initdb.d/01-RLS.sh
      - ./postgres/migration/base/02-CREATE.sql:/docker-entrypoint-initdb.d/02-CREATE.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  nats:
    image: nats:2
    restart: always
    command: ["-DVV"]

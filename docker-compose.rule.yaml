# For local testing
services:
  rule-901:
    image: tazamaorg/rule-901:${TAZAMA_VERSION}
    env_file:
      - env/rule-901.env
      - .env
    restart: always
    depends_on:
      - valkey
      - postgres

  rule-902:
    image: tazamaorg/rule-902:${TAZAMA_VERSION}
    env_file:
      - env/rule-902.env
      - .env
    restart: always
    depends_on:
      - valkey
      - postgres
